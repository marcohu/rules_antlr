package org.antlr.bazel;


/**
 * Enumeration of output formats.
 */
public enum Output
{
    /** Create a .srcjar with the generated files. */
    SRCJAR,

    /** Create a folder with the generated files. */
    FOLDER;
}
